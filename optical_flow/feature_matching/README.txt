Using this folder to start re-purposing code. The intention is to delete all the files form the parent folder and use it as the main folder. 

Data is added to a subfolder for easy access. The same data is available in a parent folder. after code re-purposing is complete only one copy of the data should be left. 